{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows Launch",
            "program": "${workspaceFolder}/bin/KATE_TESTING.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd":"${workspaceFolder}"
        }
    ]
}